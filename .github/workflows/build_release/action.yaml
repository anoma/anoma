name: build the release binaries
description: build the release binaries
runs:
  using: "composite"
  steps:
    - name: build the anoma client release 
      shell: bash 
      run: mix do --app anoma_client escript.build